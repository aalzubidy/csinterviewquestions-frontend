import React from 'react';
import { withRouter, useHistory } from "react-router-dom";

const NewPost = () => {

  return(
    <div>
      <h1>Hi from new new post</h1>
    </div>
  )
}

export default withRouter(NewPost);
